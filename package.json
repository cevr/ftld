{
  "name": "ftld",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && tsup lib/index.ts --format cjs,esm --dts",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "tsup": "^6.7.0",
    "typescript": "^5.0.4",
    "vite": "^4.2.1",
    "vitest": "^0.30.0"
  }
}
