{
  "name": "ftld",
  "private": false,
  "version": "0.11.0",
  "types": "lib/index.ts",
  "module": "dist/esm/index.js",
  "main": "dist/index.js",
  "files": [
    "dist",
    "lib"
  ],
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "types": "./lib/index.ts",
      "import": "./dist/esm/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup lib/index.ts --format cjs,esm --clean --legacy-output",
    "prepare": "npm run test && npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "tsup": "^6.7.0",
    "typescript": "^5.0.4",
    "vitest": "^0.30.0"
  }
}
